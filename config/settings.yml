virtual_category:
  virtual_category_enabled:
    default: true
    client: true
    description: "Enable virtual category functionality"
  virtual_category_max_tags:
    default: 20
    min: 1
    max: 100
    client: true
    description: "Maximum tags allowed per virtual category"
  virtual_category_max_tag_groups:
    default: 20
    min: 1
    max: 100
    client: true
    description: "Maximum tag groups allowed per virtual category"
